# E2 来源操作实现检查（2026-09-07）

## 范围

- 任务：E2/F2 导入、删除与来源重新定位的前端协调收口。
- 源码基线：`65b5ce6`；工作树同时保留本轮既有的 TODO、E1 记录及三个未跟踪目录/资产。
- 影响机制：批量选择中前项导入成功、后项出现非解析错误时，旧实现立即退出并跳过书架刷新，导致已写入的前项暂时不可见；刷新本身还会开启新的操作序号，因此失败提示不能无条件覆盖更新操作。

## 改动

- `src/stores/libraryStore.ts` 提取内部 `refreshBooks`，统一维护加载计数、列表请求顺序和操作序号。
- 批量导入保留首个错误；解析错误仍继续，在首个非解析错误处停止后续导入，但无论部分失败与否都刷新书架；刷新成功后只在没有更新操作或清错竞争时恢复该错误。
- 删除和重新定位复用同一刷新协调，保持旧列表响应不会重新引入已删除卡片的保护。
- `scripts/frontend-tests/libraryStore.test.cjs` 新增前项成功、后项失败仍刷新并保留错误的回归场景。

## 检查

| 检查 | 结果 |
| --- | --- |
| 目标缺陷红灯：`npm.cmd run test:frontend -- --library library`（修复前） | 预期 FAIL（新增场景断言失败：刷新调用次数实际 `0`，预期 `1`；既有 4 项通过） |
| library 回归：`npm.cmd run test:frontend -- --library library`（修复后） | PASS，5/5 场景通过 |
| 前端构建：`npm.cmd run build` | PASS，TypeScript 与 Vite 构建完成，204 modules transformed |

测试只替换 store 的 IPC 边界，证明前端协调逻辑，不代表真实 SQLite、SAF 或设备集成。未执行真实导入/删除/重新定位、Android SAF 撤权恢复、数据库重启恢复和完整跨端矩阵；这些场景仍由 E2 集成检查与 E10 承接。未修改 Rust、IPC 契约、readerStore 或依赖。

Astra 审阅确认取消选择不提交操作、重新定位失败不替换旧卡片、删除成功才移除卡片的接线保持原语义；本轮只变更前端刷新协调，后端身份匹配、级联和失败候选清理由冻结契约负责。此处为代码核对，不是运行态证明。按当前策略关闭 E2 实现项，E2 集成检查保持待验，下一实现项为 E3。
