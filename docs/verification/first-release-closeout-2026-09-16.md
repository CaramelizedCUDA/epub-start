# 首版归并与本地收束 — 2026-09-16

## 当前状态

维护者授权整理本地目录、将验收版本归入 main，并准备公开仓库与首个 Release。协议选择仍待决定，因此此次只完成可独立推进的归并、归档与发布材料，不改公开可见性、不发布 Release。

业务源码已提交为 `1a9175728c1b478e831ddebf3bca0a0ea3098bc7`。归并前 main 为 `4c6f28a`，开发分支为 `c121d24`，两者相差 37 个提交且无需解决分叉冲突；另两个开发分支均为当前开发历史的祖先。先将验收副本的 32 项实质差异提交，再将 main 快进到该提交，不改写已有历史。

## 证据复用

核对最终验收清单的全部 227 项源码，以及暂存内容的 Git 换行规范化后字节；无缺失文件。Rust、IPC、数据库迁移、依赖锁定文件和权限配置与既有候选没有实质变化。复用同阶段 Reader 65/65、类型检查、双平台构建、Android 静态审计及相关真实运行证据；不因路径和提交号变化重跑设备矩阵。

本次 Windows 普通应用身份重新构建通过，前端产出仍为相同的四个文件与 654,755 B；EXE 12,660,736 B、版本 0.1.0，SHA-256 `d74736d2ceb2c6259db72f4d9b76117bd5748011492f88fbdbd8f2d8e4088f03`，无 Authenticode 签名。本次不启动最终 EXE，按维护者此前决定停止扩测。Tauri 生成的 schema 与 Cargo 清单换行副作用另存本地，未混入业务提交。

对本地和已拉取的拟公开分支/标签历史检查 630 个 blob，并检查当前候选：未命中所选常见凭据模式，未发现 EPUB、数据库或签名密钥等受检扩展名。此为限定规则检查，不宣称绝对无秘密；本地 stash 与 Codex 快照 refs 不在推送范围。

## 本地布局与恢复

- `D:\epub_start`：唯一继续开发的 Git 仓库。
- `D:\epub-start-deliveries`：本地交付、验收记录、备份与发布准备，整体不纳入 Git。
- 其中 `archive/accepted-source-20260916` 保存旧验收工作副本；`archive/historical-backups` 保存原备份目录；`archive/test-extractions` 保存两个历史 EPUB 解包目录；`archive/local-work-records-20260916` 保存原仓库根目录 55 个日志/备份文件。
- 后续整理将整轮验收资料（3,218 个文件）移入 `archive/acceptance-20260914-20260916`，前后文件数和总字节一致；当前发行所需 Android 待签名 APK/AAB 另存 `release-preparation-20260916/artifacts/android` 并核对 SHA-256 一致。外层只留统一 archive、当前首版准备和资料索引。
- 未合入的 P3/P4 设计草稿保存于 `archive/unmerged-design-drafts`。旧工作区全部受版本管理文件及未跟踪文件另有 ZIP 副本和逐文件校验清单。

归并前 Git bundle、工作区 ZIP、文件哈希与目录迁移清单保存在本地 `release-preparation-20260916`。原始未提交状态还保留为本地 stash `9d7b08ea59419e680a9f6b8e018c0fd65e2e2854`；恢复应提取所需文件或另建恢复目录，不向当前 main 整体覆盖旧工作区。不删除 stash，不推送它。

旧记录中的候选绝对路径是历史位置，现由上述归档映射解释；历史记录及制品哈希不回写。源码副本内唯一指向自身的生成原生库链接已调整为相对链接，目标文件字节保持不变。用户书库、手机数据备份、历史签名材料均留在本地。

## 待完成

项目许可证、第三方发行声明、正式 Android 签名与更新兼容方案、最终文件清单，以及公开仓库和 Release。当前可发布性见 [首版说明](../releases/v0.1.0.md)，不重新开启本轮已停止的功能验收或 Reader 重构。
