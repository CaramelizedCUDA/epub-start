# E5 阅读会话隔离记录（2026-09-08）

## 范围与风险

从 `cc8f7d0` 继续，优先处理快速开关书时旧会话污染新书的问题：延迟进度、旧 `relocated` 回调、排版设置队列、自动批注读取和组件初始设置请求。切书时保留上一书待保存进度，并清空旧书瞬态状态。

沿用现有 EPUB.js 与重排模块，不修改 Rust、IPC、数据库或依赖，不提前实现其他格式。E5 的引擎集中管理及阶段恢复验收仍按 TODO 分别跟踪。

## 验证安排

Luna max 实现并执行真实 store 的定向回归及一次前端构建；另一位 Luna max 执行实际 EPUB.js DOM 的受影响开关书路径。Astra 审阅代码与证据，不重复操作。

逻辑回归替换外部边界，只证明会话隔离，不证明真实 CFI、数据库或设备行为。真实持久化、字号/窗口变化锚定和 Windows/Android 跨端恢复仍需阶段证据，最迟 E10 结清。未变化的标签、来源、缓存、Rust 及打包预算不重复验证。

## 已完成实现与逻辑检查

- `readerStore`：旧进度恢复、旧 `relocated`、设置队列与自动批注读取校验会话/目标；切书冲刷待保存进度并清空瞬态状态；打开失败清除已销毁实例引用。同会话替换 Rendition 后不销毁当前 Book，并在替换就绪后结束加载。
- `EpubReader`：初始设置和自动批注 effect 在清理后不再回写页面或重新打开旧书。
- `npm.cmd run test:frontend -- --reader`：最终 6/6 通过。前五个用例在 `cc8f7d0` 的实际 store 上均按目标断言失败，修复后通过：延迟进度、旧事件、切书冲刷/清空、旧设置、旧批注。同会话替换的第六个用例在中间修复版出现 `isLoading: true !== false`，补齐完成状态后通过；未额外注入生产缺陷。
- `npm.cmd run build`：最终源码通过。补 TSX 与同会话替换修复后重新构建有对应源码变化，不因交接重复执行。
- `npm.cmd run test:frontend -- --list`：登记 `library`、`reader`。默认按组串行构建隔离副本；未重跑未改变的 library 行为测试。

临时测试输出位于忽略的 `target/frontend-tests/`；长期断言保存在 `scripts/frontend-tests/readerStore.test.cjs`。无需设备即可再次机械执行，但不能将这些结果外推为真实正文或存储验收。

## 界面执行与停止点

Luna 在 Edge 的本地临时 fixture 中打开样本 A（`测试文档存放处/epub/6.epub`），实际组件与 EPUB.js 读取 container、OPF、导航和封面 XHTML，并创建 `about:srcdoc` iframe；settings、progress、notes、activity 使用 mock IPC，没有接触正式书库。样本 B 准备了第四卷，但未执行完整切换。

- BLOCKED（仅此浏览器入口）：阅读 iframe 实际高度为 0，截图尺寸异常；尝试下一页后仍为 `0/0`、无 CFI，无法判定真实分页与恢复。停止环境排查，未改生产布局去迁就 fixture。
- NOT_RUN：A 翻页 → B → A 恢复、延迟初始设置返回后关闭不复活、真实 SQLite 重启恢复。上述场景不能记为通过；后续在有正常可见视口的运行入口执行，最迟 E10 结清。
- 本轮 ADB 未发现在线设备；没有构建或安装 Android，不据此宣称 Android 验收结果。

## 接续提示

本轮六项逻辑回归与构建证据已有效取得，只有源码再次改变相关行为才重跑。下一步先选择能正常显示阅读正文的视口完成上述两个受影响 UI 场景，不继续反复调试侧栏零高度，也不重跑 Rust/预算/标签矩阵。随后继续 E5 的 engine 集中管理与真实 CFI/重排工作；不得凭本次修复勾选整个 E5。
