# 项目长期路线图（Backend First）

本文件规定项目的阶段顺序、冻结边界和交付门禁。当前可执行任务以 [TODO.md](TODO.md) 为准；目录职责以 [ARCHITECTURE.md](ARCHITECTURE.md) 为准；表字段以 [DATABASE.md](DATABASE.md) 为准；Command、模型和错误语义以 [IPC.md](IPC.md) 为准；依赖与验证命令以 [README.md](README.md) 为准。

## 总体决策

项目从现在起采用严格的 **Backend First** 流程：

1. 先完成 Rust 后端、SQLite、来源/协议、平台适配和 IPC 契约；
2. 后端通过“实现完成门禁”和“发布候选门禁”后，冻结后端契约；
3. 只有在后端门禁通过后，才开始重新搭建、整理和美化 React 前端；
4. 前端阶段不得反向修改后端语义来迁就 UI。发现契约缺陷时，回到后端变更流程，更新 IPC、模型、测试和迁移记录后再继续。

前端现有实现保留在工作区中，但从本路线图起视为 **legacy shell（临时壳层）**：它不再作为产品质量或阶段完成证据。后端阶段只允许为了契约同步、类型检查、安全修复和构建阻塞进行最小前端改动，不得新增前端功能、视觉方案或交互优化。

## 阶段状态

| 阶段 | 状态 | 交付目标 |
| --- | --- | --- |
| B0 后端审计与基线 | 已完成（2026-08-14） | 审计缺口关闭；迁移回滚变红自证和 Android 干净构建证据已补齐 |
| B1 后端核心能力 | 已完成（2026-08-14） | 来源、格式、协议、数据库、服务、平台和错误契约完成，首次 Android 实机门禁通过 |
| B2 后端业务能力 | 已完成（2026-08-23） | 搜索索引、系列、标签、设置、批注、后台任务以及制品/运行时存储预算均已完成真实实现与限定范围验证 |
| B3 后端验证与契约冻结 | 候选未签发 | 自动化、Windows 与 arm64 空白安装范围已关闭；37 个唯一固定样本的占用、私有 data 精确分项和正式签名仍开放 |
| B4 阅读洞察后端 | 已完成（2026-08-28） | 阅读时长、历史独立管理、足迹、继续阅读与离线推荐契约已实现；不包含生产前端 |
| F1 前端重建 | 已锁定 | 以后端冻结契约为输入，重做应用壳层和信息架构 |
| F2 前端功能实现 | 已锁定 | 按冻结 IPC 接入书架、阅读器、搜索、批注、设置和管理功能 |
| F3 前端视觉与跨端验收 | 已锁定 | 完成视觉系统、交互细节、Windows/Linux/Android 体验验收 |
| P3 多格式与分发容器 | 冻结 | 依次评估 TXT、CBZ/图片 ZIP、PDF、CBR 和通用 ZIP 分发包 |
| P4 外部网盘与远程来源 | 冻结 | 阅读器本体完成后，再评估远程书籍来源、授权、缓存和离线能力 |

## Android 分阶段实机门禁

Android 不再只作为发布前的一次性验收项。普通模拟器只能用于构建和基础行为检查，不能替代真实设备上的 SAF Provider、持久权限、OEM 进程回收、WebView、性能与手势测试。因现有真机无法安全构造近满存储，固定 API/镜像并限制 `/data` 容量的受控 Android 虚拟设备可作为一次性例外，替代 ENOSPC/SQLite 满盘、长期/2 GiB 写入及重启清理门禁；结果不得外推为真实设备性能或 OEM 行为。

| 阶段 | Android 目标 |
| --- | --- |
| B0 | SDK/NDK、Rust targets、双机和固定样本已具备；V1/V3 回滚测试完成变红自证，官方命令已从干净、无本地绕过的 scaffold 产出 debug APK/AAB。 |
| B1 | **第一次真实设备门禁已通过。** 已验证 SAF 选择、持久授权、打开、完全重启、授权撤销、重新定位、私有缓存、文件描述符生命周期和后端资源协议；未自动化的 Provider/WebView 行为继续单列。 |
| B2 | 在真实设备验证搜索/索引取消、进程被系统终止后的恢复、来源失效和大文件；低存储与长期压力按受控 Android 虚拟设备例外补测。建立 Android 发布制品与运行时存储预算并验证淘汰；受控 AVD 已补齐活动读者、来源 hard-limit、SQLite 低余量、长期压力和多记录业务/缓存恢复证据；封面公共 hard-limit 并发分支保留辅助逻辑边界。真实设备已有取消/进程终止/重建记录，未覆盖项按边界单列。 |
| B3 | 复核后端冻结前的 Android 来源链、资源预算和当前 arm64 发布候选；2026-08-24 静态候选已通过，空白安装已完成，仍待按 [B3_ANDROID_SAMPLE_MANIFEST.md](B3_ANDROID_SAMPLE_MANIFEST.md) 执行 37 个唯一样本的占用复测。更广 OEM/真实设备上的前端消费矩阵由 F2/F3 承接。 |
| B4 | 共享 Rust/SQLite 逻辑、迁移和 TypeScript 镜像完成自动验证；由于当前不接入 Reader/书架 UI，锁屏、后台、进程终止和 30 秒观察节奏的 Android 运行态消费由 F2 承接，不以模拟点击伪造完成。 |
| F2 | 在真实设备上验证书架、阅读器、目录、搜索、批注、设置、图片查看和来源重新定位等功能；承接 Android WebView 与 OEM/设备矩阵的前端消费回归。 |
| F3 | 验证触控/手势、窄屏、软键盘、深色模式、耗电、内存、不同 Android/WebView 版本和发布候选回归。 |

B1 是首次实机验证节点，现已完成；B3 未签发条件与经批准追加的 B4 共同构成进入前端前不可越过的后端门禁。B1 的后端协议检查以 Rust 协议处理器、来源读取、状态码/MIME/CORS/路径防护的直接证据为准；legacy 前端或 WebView 选择何种请求方式属于后续前端集成，不参与 B0/B1 后端完成判定。B2 受控 AVD 的低存储/长期压力、活动读者、来源 hard-limit、SQLite 低余量和限定范围业务恢复已形成可重复证据；B3 只承接当前 arm64 候选的静态、同架构安装和 37 个唯一固定样本占用复测，B4 只交付后续 UI 所需的真实数据接口；完整前端消费及更广 OEM/真实设备矩阵由 F2/F3 承接。

## B0：后端审计与基线（已完成）

目标不是继续堆功能，而是先回答“后端现在是否健康、哪些能力确实完成、哪些只是前端看起来能用”。

审计必须覆盖：

- Rust 生产代码与测试代码中的 `panic`、`unwrap`、`expect`、占位实现和错误吞噬；
- `src-tauri/src/commands` 的注册清单与 [IPC.md](IPC.md)、TypeScript 镜像的一致性；
- 数据库迁移的幂等性、失败回滚、外键级联和 V1/V2/V3 数据保留；
- 来源指纹、`SourceLease`、独立 Reader、ZIP 预算、协议 Range/MIME/CORS 和路径穿越防护；
- 服务层是否真正承载业务逻辑，Command 是否保持薄适配；
- Android SAF 的代码完整性与外部设备认证阻塞；
- 后端测试是否覆盖错误路径、取消、并发、资源耗尽和失效来源，而不只覆盖 happy path。

B0 的输出是缺口清单和每个缺口的验收标准。审计完成前，不得把已有的 `npm run build`、页面截图或前端人工操作写成后端完成证据。

## B1：后端核心能力（已完成）

本阶段只实现 Rust/SQLite/平台能力，不搭建新的 React 页面。

### 来源、格式和协议

- 统一桌面路径与 Android `content://` 来源验证、重新定位和失效状态；
- 保持 `SourceLease` + 独立 `Read + Seek` Reader，禁止整本 EPUB 长期驻留内存；
- 完成 EPUB 元数据、目录所需资源、封面和单条目资源读取；
- 完成格式注册表、`ActiveFormat`、`ResourceProvider` 和稳定的 `FORMAT_NOT_SUPPORTED:` 错误；
- 完成 `epub://`/Windows localhost 资源路由、MIME、Range、CORS、预算和路径规范化；
- 桌面图片导出必须由后端完成受控读取和系统保存对话框；Android 导出在 SAF 写入链完成前必须明确返回未支持错误。
- 在真实 Android 设备完成首次来源链门禁：SAF 选择、重启持久权限、撤销授权、重新定位、缓存和协议资源读取。

### 数据库和服务

- 追加迁移只能新增，不能改写已经发布的迁移；
- `books`、进度、批注、来源缓存、阅读设置、系列、标签和关系必须由仓储/服务层负责；
- UUID、审计时间戳和可编辑字段边界由 Rust 控制，前端不得伪造；
- 所有级联删除、唯一归属、继承去重、重排和恢复规则必须有数据库或服务测试。

## B2：后端业务能力

以下能力必须在后端先完成真实实现，不能以空 Command、stub、前端本地数组或假进度替代：

- EPUB.js 目录与阅读链所需的 `container.xml`、OPF、NAV/NCX、spine 和关联资源统一经 `ResourceProvider` 提供，并冻结 MIME、路径与错误语义；Rust 不重复解析目录树或合成 CFI；
- 同系列/多卷全文搜索所需的可取消后端任务、资源预算和结果模型（已实现，桌面自动化覆盖；Android 已复测取消、进程终止恢复、13.20 MiB 单次导入、来源失效，并以受控 AVD 补齐低存储与固定样本累计压力）；当前书章节内查找按 IPC 由 F2 的 EPUB.js 适配层负责，不属于本后端任务；
- 同系列/多卷 FTS5 trigram 索引、增量更新、来源指纹失效、手动重建和状态查询（已实现）；
- 搜索结果上限、摘要/纯文本限制、中日韩文本处理、取消和错误语义（已实现并有单元测试）；
- 系列 CRUD、单系列归属、卷标与排序（已实现并完成辅助逻辑自动化验证；桌面/Android 管理与消费 UI 留待 F2）；
- 标签组、标签、书籍直接标签、系列继承标签、筛选所需的稳定查询（已实现并完成辅助逻辑自动化验证；桌面/Android 管理与消费 UI 留待 F2）；
- 全局/单书阅读设置的持久化和字段覆盖规则（已实现并完成默认值、逐字段继承与 V3/V4 迁移回归；legacy Windows 消费已回归，新前端与 Android 消费留待 F2）；
- 批注的 CFI、长度、颜色、删除级联和缺失错误语义（已完成辅助逻辑自动化收口；legacy Windows 选区/增删改已回归，新前端高亮/恢复与 Android 消费留待 F2）；
- Android SAF 权限重启校验、失效来源处理；能在目标设备上验证的导出/手势能力也必须在此阶段完成后端链路。
- Android 搜索/索引任务已在黑鲨 Android 9 的当前 APK 验证取消、进程终止恢复、系统 picker 的 13.20 MiB 单次导入、超大章节拒绝和来源授权撤销后的索引失效；七卷重建期间记录主库与 rollback journal 占用。缓存存储耗尽映射已用内存错误对象自证，SQLite `SQLITE_FULL` 已用受限 page_count 的真实写事务自证并验证回滚保留旧索引；受控 AVD 已补齐 ENOSPC、复制中断/重试、单书后台索引、六轮固定 fixture 累计 2 GiB 逻辑输入和限定范围综合恢复，但不外推真实设备性能、唯一物理 2 GiB、多样内容或完整业务数据。

### Android 制品与运行时存储预算

体积治理必须区分三类数据，禁止把 Android 设置页显示的单一“应用大小”直接当作安装包结论：

1. **发布制品**：arm64 release APK/AAB、其中的 Rust 原生库和前端 `dist`；
2. **安装后代码**：系统解包、对齐和运行时优化后的 code 占用；
3. **运行时数据**：数据库、Android 私有来源缓存、封面缓存和搜索索引。

B2 采用以下初始门禁，首次可重复 release 基线建立后只允许收紧；放宽必须记录原因并经人工批准：

- arm64 release APK 不超过 40 MiB，Rust 原生库不超过 30 MiB，前端 `dist` 不超过 2 MiB；相对已提交基线增长超过 10% 即使仍低于绝对上限也必须解释；
- debug APK 只用于诊断，不作为发布体积证明；profile 使用 release Rust、独立包名、可调试应用、关闭 JNI debug/R8 和 debug 签名，供日常诊断/AVD 验收；release 不得携带 Rust/NDK 运行时调试段、测试 EPUB 或预置来源缓存；
- Android 来源缓存软上限 256 MiB、硬上限 512 MiB；封面缓存软上限 64 MiB、硬上限 128 MiB；搜索索引文本账面硬上限 256 MiB，当前统一常量合计 896 MiB、不超过 1 GiB；补充 6 GiB AVD 已记录 SQLite 低余量主库/rollback journal 峰值，WAL/SHM 未观察到，不把未使用的日志模式写成已通过场景；
- 来源缓存命中必须更新 `last_accessed_at` 并按持久化真实 LRU 淘汰，活动 `SourceLease` 不得被删除。封面按 `books.updated_at, id` 确定淘汰顺序；asset 读取不可观测，不宣称命中 LRU。无法在硬上限内安全完成写入时返回稳定资源限制错误，不得静默突破上限；
- 记录空白安装、固定样本导入、触发淘汰、进程重启和清理后的分项占用；覆盖磁盘不足、中断写入、孤儿封面/索引清理和缓存重建。数据库与用户明确保存的内容不是缓存，不得为达成指标而删除。

若需要暴露缓存统计或清理能力，先在 [IPC.md](IPC.md) 设计 Command、返回模型和错误语义，再实现和注册；本规划不授权预注册 stub。数据库应优先复用 `source_cache_entries.cache_size_bytes` 与 `last_accessed_at`，确有字段缺口时只能追加迁移。

2026-08-23 最终复核状态：来源/封面预算、重启协调、孤儿清理、稳定存储错误、来源租约/淘汰竞态闭合、封面并发候选预算和 896 MiB 实际硬预算（受 1 GiB ceiling 约束）均已实现并完成辅助逻辑变红自证；arm64 release 最终静态基线为 APK 11,557,632 B、AAB 11,365,807 B、Cargo release `.so` 13,040,288 B、打包 `.so` 8,951,720 B、`dist` 595,644 B。门禁核对完整工具链及 APK/AAB 各自的 ABI/ELF 后通过。受控 AVD 已补齐活动读者与后台索引并发、来源 hard-limit、ENOSPC、SQLite 低余量、六轮长期压力和 4 本书/2 个系列/标签/设置/进度/批注及可重建来源/封面/索引恢复；真实黑鲨 Android 9 的取消、进程终止与重建记录直接接受。完整 Gradle release lint 同日以 0 error 通过。封面公共 hard-limit 并发分支只观察到 admission 串行化/软淘汰，更广 OEM/真实设备矩阵移至 B3/F2；具体证据见 [ANDROID_STORAGE_ACCEPTANCE.md](ANDROID_STORAGE_ACCEPTANCE.md)。

2026-08-22 受控 AVD 首轮复测状态（修复前）：新增 `custom-protocol` Cargo feature 别名并修正验收文档的实际 `app_data_dir` 路径；新 profile 包空白安装与 46 份逐份导入成功，来源/封面预算观察、缺失元数据协调和孤儿清理有证据。Android WebView 拒绝当前 `epub:///localhost/...` URL，活动读取/后台索引未通过；ENOSPC、复制中断和六轮 2 GiB 压力仍未完成，B2 总门禁继续未签发。
2026-08-22 受控 AVD WebView 修复复测：Android 根地址切换为 `http://epub.localhost/...`，前端在 EPUB.js 请求边界归一化 `null/...`、`epub://localhost/...` 和 `epub:///localhost/...`。重新编译并安装 x86_64 profile APK 后，固定 EPUB 重新定位及单书首屏读取通过，输出 `WEBVIEW_REGRESSION=GREEN`，未出现不支持 scheme、`Failed to fetch`、`epub:///` 或 `localhost:1420`；证据为 `target/android-b2-acceptance-20260822-184117/reader-after-final-fix.png`。后台索引、ENOSPC、复制中断、清理重建和六轮 2 GiB 压力仍未完成，B2 总门禁继续未签发。
2026-08-22 受控 AVD B2 收口补测：143 MB 有效 EPUB 在 `copy_source_atomically start` 后 force-stop，重启清理 `.source.tmp` 且重试成功；全新 profile 在约 4 MiB 可用空间下导入新 locator，UI 返回 `BOOK_RESOURCE_LIMIT_EXCEEDED: source cache copy failed because device storage is full`，清理填充文件后无临时文件；删除 `source-cache/` 与 `covers/` 后单书来源/封面从持久 SAF 来源重建，数据库完整性保持 `ok`。同一 AVD 的临时 closeout debug APK 新增可观察后台索引入口，单书测试系列任务最终 `34/34 ready`，SQLite `integrity_check=ok`、FTS=34。证据目录为 `target/android-b2-closeout-20260822`。六轮 2 GiB、完整业务数据重建和来源/封面 hard-limit 运行态当时尚未执行，B2 总门禁继续未签发。
2026-08-23 受控 AVD B2 收口补测：`target/android-b2-pressure-20260823-direct` 完成 6 轮×46 份固定 EPUB fixture 的 direct IPC/SAF 导入，累计逻辑输入 `2,352,070,068` 字节（约 2.19 GiB）；每 15 份导入重启 profile、轮末 force-stop/restart，第 6 轮保留 30 分钟后清理，日志以 `LONG_PRESSURE_DONE` 正常结束。`target/android-b2-recovery-20260823` 通过持久化 SAF locator 的真实重新导入，验证 1 本书、1 个系列、1 条关系和 `34/34 ready` 索引保留，同时来源/封面缓存可重建；启动协调单独运行不会凭空生成缓存。该压力使用同一固定样本，不证明唯一物理 2 GiB、多样内容或完整多记录业务恢复；来源/封面 hard-limit、并发活动读取、取消/重建分支与 SQLite page/WAL 低存储峰值仍未覆盖，B2 总门禁继续未签发。完整 Gradle release lint 已在同日单独收口。

2026-08-24 B3 Windows 边界复核：开发态与 x64 release 已覆盖打开、资源链、目录、搜索、批注、设置、进度、全屏和基础重启；隔离 fixture 已补齐导入、删除和失效来源重新定位。系列/标签的真实 Rust/IPC/事务契约由自动化覆盖，管理/消费 UI 按本路线图留给 F2，不再循环阻塞 B3；记录见 [B3_WINDOWS_RUNTIME.md](B3_WINDOWS_RUNTIME.md)。

EPUB.js 的 DOM 选区、目录树渲染、CFI 视口恢复和视觉交互属于 F 阶段；B 阶段只交付它们依赖的稳定数据和 IPC 契约，不提前实现 UI。

## B3：后端验证与契约冻结

后端只有同时满足以下条件，才可进入 F1：

1. `cargo fmt --check`、`cargo check`、完整 `cargo test` 全部通过；
2. 数据库空库、升级、重复启动、失败回滚、级联和数据保留测试通过；
3. 来源失效、重新定位、多候选拒绝、ZIP bomb、路径穿越、Range/MIME/CORS、错误脱敏测试通过；
4. 所有已交付 Command 都有 IPC 文档、Rust serde 模型、TypeScript 镜像和调用契约；未实现能力不注册 stub；
5. 搜索/索引后台任务具备取消、状态、上限、预算、错误和重建行为；
6. Windows 运行态完成 legacy shell 实际具备的导入、打开、资源协议、删除、重新定位、批注、设置、搜索和重启恢复回归；系列/标签必须具备真实 Rust/IPC/事务契约与自动化覆盖，其管理/消费 UI 在 F2 按冻结契约实现，不得要求先实现 F2 才允许 B3 通过；
7. Android 代码链已完成静态审计，并在真实设备通过 B1 来源链门禁；B2 的后台任务、活动读者、来源 hard-limit、缓存淘汰、SQLite 低余量、长期压力和多记录业务/缓存恢复已形成限定范围证据，封面公共 hard-limit 并发分支继续保留辅助逻辑边界；更广 OEM/真实设备的前端消费矩阵由 F2/F3 承接，不重复作为 B3 后端冻结前置；
8. arm64 release APK、原生库、前端 `dist` 与安装后/运行时分项占用均有可重复基线，满足绝对上限与 10% 回归门禁；缓存/索引软硬上限、LRU、活动租约和清理语义已经验证并冻结；
9. [README.md](README.md)、[ARCHITECTURE.md](ARCHITECTURE.md)、[DATABASE.md](DATABASE.md)、[IPC.md](IPC.md)、[CONVENTIONS.md](CONVENTIONS.md)、[TODO.md](TODO.md) 和 [SECURITY.md](SECURITY.md) 与实现一致。

通过后建立“后端契约冻结候选”。2026-08-28 用户在其正式签发前明确批准追加 B4，因此 `B3_CONTRACT_FREEZE.md` 保留为历史候选快照，最终冻结输入必须包含 B4。

## B4：阅读洞察后端

B4 在不实现生产前端的前提下补齐已确认的书架数据需求：

1. 以 Reader 正文前台可见为计时条件，同页停留继续计时，锁屏/后台/离开 Reader 暂停；约 30 秒确认一次，超过 90 秒的未知间隔和进程退出后时间不补算。
2. 追加 V5，把可删除的活动/可见区段历史与继续阅读所需的图书阅读状态分开；删书保留脱离实时图书关联的历史快照，删历史不影响图书、进度或继续阅读。
3. 提供日/周/月/季度阅读时长桶、最近图书及进度、按年/总计足迹；后端只返回中性数据，不决定时间轮盘、热力颜色或桌面/Android 排版。
4. 首版离线推荐只覆盖系列下一本、较久未读完、未开始三种候选，返回结构化理由并由前端本地化。正文/精彩文段推荐延后到 Reader 完成后的单独评审，不依赖当前搜索索引，也不注册 stub。
5. 完成 V5 空库/升级/幂等/失败回滚、活动状态机/序列幂等/异常恢复、自然日期与时区边界、历史独立删除、删书后快照、聚合与推荐的自动化变红自证；同步 Rust/IPC/TypeScript 49 个真实 Command，并诚实保留桌面/Android 前端消费未测范围。

## F1：前端重建（B3 未签发条件与 B4 完成之后）

- 删除/替换 legacy shell 的布局假设，先建立清晰的应用壳层、导航、空状态、加载状态和错误状态；
- 只消费已冻结的 IPC，不在 React 中解析 ZIP/XML/SQLite 或推断来源有效性；
- 先完成信息架构和可用性，再进入颜色、字体、动效和视觉细节；
- 当前前端的已完成标记不自动继承为新前端的验收结果。

## F2：前端功能实现

按后端契约接入书架、来源重新定位、阅读器、目录、搜索、批注、系列、标签和阅读设置。EPUB.js 的 DOM 解析、CFI 视口锚定、iframe 事件和交互状态集中在 `src/features/reader/engine/`，不把业务逻辑重新塞回 Rust。

## F3：前端视觉与跨端验收

完成 Tailwind 视觉系统、响应式布局、可发现控件、键盘/触摸补充交互、空/错/加载状态、深色模式和 Windows/Linux/Android 运行态验收。视觉质量必须通过真实运行态和人工验收确认，构建通过不等于体验完成。

## P3：多格式与分发容器（冻结区）

P3 只有在 EPUB 阅读器本体的 B0–B3、F1–F3 完成后才解锁。目标是在不污染 EPUB 专用语义的前提下，支持常见小说、轻小说和漫画格式。项目必须区分两类阅读模型：

| 阅读模型 | 格式 | 主要能力 |
| --- | --- | --- |
| 可重排文档 | EPUB、TXT | 字体、字号、行距、重排、文本搜索、文本选区与批注 |
| 固定页面 | PDF、CBZ、CBR | 页码、缩放、适宽/适高、单双页、连续页面和左右阅读方向 |

PDF 使用固定页面模型，但文本层、目录、搜索和选区能力必须按文件实际能力声明。扫描版 PDF 不得伪装支持文本搜索或文本批注。前端不应通过散落的 `format === ...` 分支决定功能，而应消费格式能力；不支持的能力不显示入口，不返回虚假空结果。

### P3.0：多格式架构与导入检查

进入任何新格式实现前，先完成设计评审，不引入依赖、不创建占位处理器：

- 定义共享能力边界：`MetadataProvider`、`ResourceProvider`、`TocProvider`、`TextContentProvider`、`PageProvider`、`SearchProvider` 和 `AnnotationProvider`；
- 定义通用位置模型：EPUB CFI、TXT 文本锚点、PDF/漫画页码及必要的页内坐标，不把所有格式继续塞进 `location_cfi`；
- 设置按阅读模型分组：文本排版设置不得出现在漫画模式，页面缩放/阅读方向不得污染 EPUB/TXT 设置；
- 设计 `ImportInspector`：从桌面路径、Android SAF 或未来 P4 缓存取得受控 Reader，识别一个或多个导入候选，再交给格式注册表；
- 明确 EPUB 虽然是 ZIP 容器，但必须先按 EPUB 签名和 `mimetype`/`container.xml` 识别，不能被通用 ZIP 分发检查误判；
- 评估重型前端代码动态加载和 Rust Cargo Feature；只有存在真实获批实现时才拆分，不为空处理器预建 Feature。

计划数据流：

```text
桌面路径 / Android SAF / 未来远程受控缓存
                    -> ImportInspector
                    -> ImportCandidate[]
                    -> FormatRegistry
                    -> EPUB / TXT / PDF / CBZ / CBR
                    -> SourceLease / Reader / Protocol
```

ZIP 在此模型中只能是“分发容器”或“具有漫画语义的图片归档”，不得新增 `BookFormat::Zip`。

### P3.1：TXT（首个新增格式）

- 评估 UTF-8、GBK、Big5、Shift-JIS、BOM 与错误编码处理；新增编码依赖必须审批；
- 使用流式读取和索引，禁止大文件一次性读入内存；
- 设计稳定文本锚点、内容变化失效、章节标题启发式、目录缺失、封面和元数据回退；
- 验证搜索、批注、进度恢复和超大文件预算，以证明可重排阅读模型不依赖 EPUB.js CFI。

### P3.2：CBZ 与纯图片 ZIP 漫画

- `.cbz` 直接按漫画归档检查；仅含受支持图片的 `.zip` 可在检查后提示“以漫画形式导入”，存储格式语义为 `cbz`，不存储为 `zip`；
- 内容识别不能只信任扩展名或 MIME，必须检查归档条目；
- 实现自然排序、封面识别、单页/双页、连续纵向、从左到右/从右到左、封面单页和跨页对齐；
- 设计相邻页面预加载、内存淘汰、损坏图片局部降级、单图尺寸/总像素/解码内存预算；
- 复用现有 ZIP 路径与解压安全原则，但不得以已有 `zip` crate 为由跳过 P3 审批和漫画专项安全测试。

### P3.3：PDF

- 单独比较 Web 渲染、原生渲染和混合方案，审批新增依赖与许可证；
- 第一版优先交付页面渲染、页码、缩放、适宽/适高、单双页和目录；
- 文本搜索、选区和批注只在可靠文本层存在时启用；扫描版 PDF 不承诺 OCR；
- 验证复杂字体、大文件、恶意 PDF、各平台渲染差异和内存预算。

### P3.4：CBR

- 在 CBZ 页面阅读模型稳定后再实现，只新增经过审批的 RAR 归档能力；
- 评估 RAR4/RAR5、许可证、密码归档、恶意归档、解压预算和各平台构建；
- 首版可明确拒绝加密归档，不得以不完整兼容宣称 CBR 全支持。

### P3.5：通用 ZIP 分发包

网络平台下载的 `.zip` 可能只是书籍分发包装，导入检查应按以下规则分类：

- 只包含一本受支持的 EPUB/TXT/PDF：安全解包到应用私有暂存区，再交给对应格式处理器；
- 包含多本受支持图书：显示导入候选预览并提供逐项结果；
- 只包含图片：交给 P3.2，以 CBZ 漫画语义导入；
- 图片、图书和附件混杂：第一版要求用户明确选择，无法无歧义分类时安全拒绝；
- 嵌套归档默认最多检查一层；加密、符号链接、绝对路径、路径穿越、重名/大小写冲突或超预算归档拒绝导入；
- 解包只能进入应用私有暂存区，成功后原子转入受控缓存，失败/取消必须清理；多书导入不得留下半完成数据库状态。

通用 ZIP 分发包排在 TXT、CBZ、PDF、CBR 之后，只有各目标格式的处理器和导入候选模型稳定后才实现。

## P4：外部网盘与远程来源（冻结区）

本阶段用于接入 WebDAV、厂商网盘或其他远程文件来源，但不属于当前 B0–B3 后端、F1–F3 前端或阅读器本体的交付范围。只有在 B3 后端契约冻结、F1–F3 前端重建与阅读体验验收以及 P3.0–P3.5 多格式/导入链完成后，才允许开始技术评估；不得因未来可能支持网盘而推迟或过度泛化当前本地阅读器实现。

### 能力边界

外部网盘首先被视为“远程书籍来源”，目标是让用户选择远程 EPUB，并通过受控缓存交给现有来源、格式和资源链读取。它不自动包含阅读进度、批注、设置或书架数据库的跨设备同步；应用数据同步若未来需要，必须另立阶段和冲突模型，不得与远程文件访问混为一体。

### 进入前置

- 本地 EPUB 的导入、打开、阅读、搜索、批注、设置、系列、标签、重新定位和重启恢复已经完成自动化与运行态验收；
- Windows、Linux、Android 的本地来源生命周期已经稳定，现有 `SourceLease`、Reader、缓存和资源预算有明确基线；
- 已提交首批 Provider 范围与技术选型，并说明优先采用标准协议还是厂商 API；
- 所有新增网络、OAuth、安全存储或 Provider SDK 依赖已经获得人类批准，并同步更新 README 白名单和 CONVENTIONS；
- 已完成数据库迁移、IPC、威胁模型、隐私说明、离线行为和失败恢复方案。

### 必须单独设计的事项

- OAuth/设备码/应用密码等授权方式，以及令牌刷新、撤销和安全存储；
- Provider、远程文件 ID、版本号/ETag、路径变化和账号失效的稳定标识；
- “下载到应用受控缓存后阅读”与“远程 Range/流式读取”的取舍。默认优先评估受控本地缓存，避免 EPUB ZIP 随机读取直接放大网络请求；
- 离线可用、缓存配额、清理策略、下载取消、断点续传、重试、限流和并发；
- 远程删除、覆盖、版本变化、同名文件和本地缓存过期时的来源状态；
- 网络错误、授权错误、配额错误和 Provider 错误的稳定脱敏语义；
- 用户隐私、遥测边界、日志脱敏，以及账号退出后的凭据与缓存清理。

### 当前冻结规则

- B0–F3 不新增远程 `source_kind`、网盘账号表、令牌字段、网盘 Command、Provider 适配器或远程浏览 UI；
- 不在当前 Schema 中预埋访问令牌、OAuth 状态或未经设计的通用 JSON 凭据字段；
- 不添加 HTTP/OAuth/WebDAV/网盘 SDK 依赖，不注册 stub Command，不以本地目录模拟远程 Provider 并宣称能力完成；
- 当前来源抽象只需正确服务桌面路径与 Android SAF，不为未选型的网盘进行猜测式重构。
