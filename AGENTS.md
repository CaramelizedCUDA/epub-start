# Project memory

## Notes

- 在进行计划和代码相关操作（增删，更改等）操作时，请务必参考README.md,ARCHITECTURE.md,DATABASE.md,IPC.md,CONVENTIONS.md,ROADMAP.md,TODO.md中的一系列项目规范以及流程指引，当发现即将进行的计划方案或操作与这些文档相悖时，格式规范类的冲突一律打回，但相关代码实现方式如果已经改无可改，可以向用户申请一次性的更改项目规范文档并且同步进行代码修改的操作，在申请时，应向用户提前声明更改内容以及即将引入的新内容，同时告诫用户可能会发生的后果

## 验证诚实规则（Verification Honesty）

以下规则适用于所有代码任务，任务收尾前必须逐条自检，未满足不得声称完成。

### 1. 测试必须自证有效（变红验证）
- 编写任何测试时，必须执行"变红验证"：故意注入一个 bug，确认测试确实失败（变红），再移除注入，确认恢复（变绿）。
- 未做自证的测试，不得在结论中声称"通过"。禁止使用无论代码是否正确都会通过的测试充当验证结果。
- 特别地，回滚/异常路径测试必须验证"冲突真的发生在目标步骤"，而不是在早期步骤就必然失败导致测试假阳性。

### 2. "已验证"必须有覆盖清单
- 在 BACKEND_AUDIT.md / TODO.md 等文档中标注"已验证/已覆盖/有测试"时，必须同时列出：测了什么 + 没测什么。没有覆盖清单的标注视为夸大，禁止写入。
- 运行态验证与静态审查必须分开标注，按实际状态标记为以下三类之一：
  - 辅助逻辑（静态/单元可自动验证）
  - 桌面端（需人工运行态验证，标注"待人工验证"）
  - Android 环境（受环境阻塞，标注"阻塞"）

### 3. 统计类数字必须自动同步
- 代码统计数字（如 `.unwrap()` 数量）禁止手写进文档。
- 任务收尾时运行 `npm run audit:unwrap`，用脚本输出核对并同步 BACKEND_AUDIT.md / TODO.md 中的对应数字，确保文档与代码一致。
