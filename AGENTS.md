# 项目 Agent 执行规范

所有模型遵守同一产品契约、权限、用户数据与资源保护规则，以及 IPC/Schema 同步、依赖白名单、冻结范围和证据诚实要求。[验证策略](docs/VERIFICATION.md) 是验证范围、升级条件、证据复用和停止条件的唯一来源；[开发指南](docs/DEVELOPMENT.md) 提供命令。

## 任务与授权

用户指定的工作优先；没有指定时，按 [TODO](TODO.md) 选择首个未完成且无阻塞的实现项。历史归档不参与排序，待阶段验收不阻塞独立实现。

开始前检查工作区，保护已有改动和未跟踪文件。Git 写操作由用户处理，除非当前会话明确授权；编辑授权不包含提交、推送或分支操作。

依赖白名单只看 [CONVENTIONS](CONVENTIONS.md)；README 产品方向不构成新依赖或新格式授权。

## 按影响面阅读

用 [文档索引](docs/README.md) 定位资料，不要求每次首次进入全文通读所有文档。当前目标与完成标准看 TODO 相关项；产品状态看 README；阶段与冻结范围看 ROADMAP；实现规则与依赖看 CONVENTIONS；模块边界看 ARCHITECTURE；Schema 看 DATABASE；Command/模型/错误看 IPC；安全、来源、权限和数据保护看 SECURITY；领域术语看 CONTEXT 和相关 ADR。

同一上下文中复用仍有效的阅读与验证证据。跨边界变更应补读相关契约，不能以精简流程为由遗漏。

## 不随模型变化的实现边界

- 只修改当前任务所需内容。F 阶段消费冻结 IPC，不预建 TXT/PDF/漫画处理器、通用位置、占位 Command 或新依赖；P3/P4 按 ROADMAP 审批。
- 修改 Command、共享模型或错误语义时，同步 Rust、IPC、TypeScript models/ipc、tauri wrapper、调用点及相关测试。Schema 只追加迁移，不改写已发布迁移。
- SQL 不放入 Command；来源校验、ZIP 和数据库解析不放入 React；EPUB.js DOM/CFI/渲染遵守 ARCHITECTURE。不通过放宽安全、依赖白名单、权限、资源预算，或删除、重置用户数据来消除检查失败。
- 构建前确认目录、目标平台、构建类型与诊断 feature；Android 静态审计先生成本次对应制品，旧 APK 或桌面构建不代表新 Android 制品。
- 按 VERIFICATION 执行本次影响面检查。不能说明具体传播机制时，不扩大验证范围；有关必跑项通过且无未解决相关失败后停止。

## 执行者、模型与交接

当前执行者模型由宿主实际选择提供，用户指定优先；模型自述、配置文件默认值和提高 reasoning 等级都不能改变身份。已确认的 GPT-6 Astra 可自主选择精简执行方法，但与所有模型遵守同一契约、验证矩阵和收尾要求。Sol/Terra/Luna、其他非 Astra 或型号不明的执行者（主代理或子代理），在相关任务开始前必须读取 [结构化执行补充](docs/agents/structured-execution.md)；按 [模型路由](docs/agents/model-routing.md) 选择可用模型与推理等级。模型切换或新子代理重新判断适用流程，不继承 Astra 的精简豁免；不能把非 Astra 提高 reasoning 等同于 Astra。

主代理负责影响面、任务切分和结果审阅。仅当子任务边界清楚、交接成本合理且有并行或独立审阅价值时才委派；委派可选且必须在当前授权范围内，不创建用户侧新任务替代内部子代理。派工携带模型、推理等级、允许文件、相关规范、必跑检查、可复用证据、未授权动作和停止条件；不因交接或模型切换重跑未失效的检查。

TODO 分别记录实现与阶段验收。编译、mock、截图不替代真实运行态；设备可用未测写待阶段验收，确有环境障碍才写阻塞。新增或改变测试行为时按验证策略证明其能发现目标故障，不对旧测试重跑反复注入。

Rust 源码或当前统计登记变更时执行 unwrap 统计与一致性核对；其他任务不重复统计，历史数字不回写。检查日志集中于同阶段 verification 记录，不为每个按钮或无变化复跑创建长报告。

收尾简述改动用途、实际检查、复用证据和未覆盖范围。用户明确批准的规范调整按授权执行；需要超出既有契约、依赖或权限时，说明具体规则和实际影响，申请必要的一次调整。
