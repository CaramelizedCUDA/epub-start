# 项目 Agent 执行规范

本文件适用于所有执行模型，包括主要承担代码与构建工作的 Luna。更换模型不降低规范、测试或验收要求；不得仅凭任务摘要跳过下述入口。

## 开始任务

1. 阅读 [README](README.md)、[TODO](TODO.md)、[ROADMAP](ROADMAP.md)、[ARCHITECTURE](ARCHITECTURE.md)、[DATABASE](DATABASE.md)、[IPC](IPC.md)、[CONVENTIONS](CONVENTIONS.md) 和 [SECURITY](SECURITY.md)。涉及领域词汇时再读 [CONTEXT](CONTEXT.md) 及相关 ADR。
2. 从 TODO 当前执行区选择第一个未完成且无阻塞的任务，先说明任务编号、改动范围和完成标准；历史归档中的复选框不是待办，不重做已记录完成的后端阶段。
3. 检查工作区已有改动，保护未提交/未跟踪文件。Git 写操作仍由用户处理，除非当前会话明确重新授权；文档/代码编辑授权不自动包含提交、推送或分支操作。
4. 依赖白名单唯一来源是 CONVENTIONS，命令唯一入口是 [开发指南](docs/DEVELOPMENT.md)，文档职责见 [索引](docs/README.md)。README 中的产品方向不是依赖或新格式实现授权。

## 实现与构建检查点

- 只修改当前任务所需内容。F 阶段整理 EPUB Reader 时保留既有 IPC，不预建 TXT/PDF/漫画处理器、通用位置字段、占位 Command 或新依赖；P3/P4 按 ROADMAP 审批后实施。
- 修改 Command、共享模型或错误语义时，同一变更同步 Rust、IPC、TypeScript models/ipc、tauri wrapper、全部调用点和测试。Schema 只追加迁移，已发布迁移不可改写。
- 不把 SQL 放入 Command，不把来源校验、ZIP 或数据库解析放入 React；EPUB.js 的 DOM/CFI/渲染按 ARCHITECTURE 管理。不通过放宽白名单、预算或安全规则消除构建失败。
- 构建前确认工作目录、目标平台、构建类型及是否需要诊断 feature。使用开发指南的仓库根目录命令；Android 静态审计前先生成同版本制品，不能拿旧 APK 或桌面构建代替本轮 Android 证据。
- 新增或修改测试均做下面的变红自证：记录注入点、目标失败断言、移除注入后的结果。失败必须发生在目标路径；验证结束清除故意注入的缺陷。

## 收尾与交接

- 按改动范围完成检查，运行 `npm run audit:unwrap`、同步当前统计并运行 `npm run audit:check`；不能把测试/编译未执行写成通过。
- TODO 只有在整个条目的完成标准满足后才勾选。部分完成时保留未勾选，记录已实现、已测、未测和具体阻塞；需要设备时不得改写为“静态通过所以完成”。
- 交接必须给出：任务编号、改动文件及用途、实际执行命令与结果、新测试变红/变绿证据、桌面/Android 覆盖与未测项、剩余任务。详细证据放 `docs/verification/`，TODO 保留摘要与链接。
- 仅文档整理执行文档链接、差异与统计核对，不制造代码变动或设备验收结论；历史证据保留原日期和适用范围。

## Notes

- 在进行计划和代码相关操作（增删，更改等）操作时，请务必参考README.md,ARCHITECTURE.md,DATABASE.md,IPC.md,CONVENTIONS.md,ROADMAP.md,TODO.md中的一系列项目规范以及流程指引，当发现即将进行的计划方案或操作与这些文档相悖时，格式规范类的冲突一律打回，但相关代码实现方式如果已经改无可改，可以向用户申请一次性的更改项目规范文档并且同步进行代码修改的操作，在申请时，应向用户提前声明更改内容以及即将引入的新内容，同时告诫用户可能会发生的后果

## 验证诚实规则（Verification Honesty）

以下规则适用于所有代码任务，任务收尾前必须逐条自检，未满足不得声称完成。

### 1. 测试必须自证有效（变红验证）
- 编写任何测试时，必须执行"变红验证"：故意注入一个 bug，确认测试确实失败（变红），再移除注入，确认恢复（变绿）。
- 未做自证的测试，不得在结论中声称"通过"。禁止使用无论代码是否正确都会通过的测试充当验证结果。
- 特别地，回滚/异常路径测试必须验证"冲突真的发生在目标步骤"，而不是在早期步骤就必然失败导致测试假阳性。

### 2. "已验证"必须有覆盖清单
- 在 BACKEND_AUDIT.md / TODO.md 等文档中标注"已验证/已覆盖/有测试"时，必须同时列出：测了什么 + 没测什么。没有覆盖清单的标注视为夸大，禁止写入。
- 运行态验证与静态审查必须分开标注，按实际状态标记为以下三类之一：
  - 辅助逻辑（静态/单元可自动验证）
  - 桌面端（需人工运行态验证，标注"待人工验证"）
  - Android 环境（受环境阻塞，标注"阻塞"并写明原因；设备可用但尚未执行时写"待人工验证"；已执行只标记具体设备与已测范围）

### 3. 统计类数字必须自动同步
- 代码统计数字（如 `.unwrap()` 数量）禁止手写进文档。
- 任务收尾时运行 `npm run audit:unwrap`，用脚本输出核对并同步 BACKEND_AUDIT.md / TODO.md 中的对应数字，确保文档与代码一致。
