# 项目 Agent 执行规范

适用于 Astra、Luna 及其他执行模型。验证范围与停止条件以 [验证策略](docs/VERIFICATION.md) 为唯一来源；更换模型既不降低正确性要求，也不增加验证门禁。

## 开始任务

1. 首次进入项目阅读 [README](README.md)、[TODO](TODO.md)、[ROADMAP](ROADMAP.md)、[ARCHITECTURE](ARCHITECTURE.md)、[DATABASE](DATABASE.md)、[IPC](IPC.md)、[CONVENTIONS](CONVENTIONS.md)、[SECURITY](SECURITY.md) 和验证策略。同一任务上下文仍有效时，后续只读变更过或与本次影响面有关的章节，不逐轮重读。涉及领域词汇时再读 CONTEXT 与相关 ADR。
2. 用户指定的工作优先；否则按 TODO 选择第一个未完成且无阻塞的实现项。历史归档不参与排序；已完成实现、等待阶段设备验收的条目不阻塞后续独立实现。
3. 检查工作区，保护已有改动与未跟踪文件。Git 写操作由用户处理，除非当前会话明确授权；编辑授权不自动包含提交、推送或分支操作。
4. 简述任务编号、改动范围、可能破坏的行为及最小必跑检查。依赖白名单只看 CONVENTIONS，命令只看 [开发指南](docs/DEVELOPMENT.md)。README 产品方向不构成新依赖或新格式授权。

## 实现边界

- 只修改当前任务所需内容。F 阶段保留既有 IPC，不预建 TXT/PDF/漫画处理器、通用位置字段、占位 Command 或新依赖；P3/P4 按 ROADMAP 审批。
- 修改 Command、共享模型或错误语义时，同步 Rust、IPC、TypeScript models/ipc、tauri wrapper、调用点及相关测试。Schema 只追加迁移，不改写已发布迁移。
- SQL 不放入 Command，来源校验、ZIP 和数据库解析不放入 React；EPUB.js DOM/CFI/渲染按 ARCHITECTURE 管理。不通过放宽安全规则、依赖白名单或资源预算消除失败。
- 构建前确认目录、目标平台、构建类型与诊断 feature。需要 Android 静态审计时，先生成本次对应制品；旧 APK 和桌面构建不能代表新 Android 制品。
- 除非能说明“本次改动可能通过什么机制破坏这个能力”，否则不得自行扩大验证范围。建议项不自动成为硬门禁；相关必跑项通过且无未解决风险后停止验证。

## 收尾与交接

- 按验证策略执行本次必跑项；只有新增或改变测试行为时做目标变红/变绿自证，已有测试重跑不重复注入。原缺陷复现可替代人工注入，具体见验证策略。
- TODO 分别记录实现状态和阶段验收状态。实现完成不能写成全平台验收通过；待阶段验收不能被改写成代码未完成。只有对应状态的完成标准满足才勾选。
- 简短交接包括改动用途、实际检查及结果、复用证据、未覆盖或阻塞范围。命令日志和新测试红绿证据集中放 docs/verification/ 的同阶段记录，不为每个按钮或每次无变化复跑新建长报告。
- Rust 源码或当前统计登记变更时执行 unwrap 统计与一致性核对；纯前端、样式及无统计变更的文档任务不重复后端统计。数字由脚本取得，历史数字不回写。
- 编译、mock 测试、截图不替代真实运行态。设备可用但未测写“待阶段验收”；确有环境障碍才写“阻塞”并说明原因。只声明实际平台、设备和场景的覆盖。

## 规范冲突与协作

用户明确批准的规范调整按其授权执行；除此之外，不以实现困难为由悄悄改变契约、依赖或安全边界。确需例外时先说明具体规则、拟改内容和实际影响，申请一次性调整。

Astra 负责影响面、任务切分和结果审阅；Luna max 按同一最小验证范围实现与检查。交接传递任务、允许文件、相关规范、必跑命令和停止条件；不得因交接或模型切换重跑未失效的证据。任务不要求双模型各跑一遍相同检查。
