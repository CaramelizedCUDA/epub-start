{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-dialog",
  "description": "Desktop-only native file open and save dialogs",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["dialog:default", "dialog:allow-open"]
}
